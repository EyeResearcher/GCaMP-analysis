name: cascade2p-legacy
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.7       # or 3.8 if supported
  - tensorflow=2.3.*
  - keras=2.3.*
  - numpy=1.18.*
  - ruamel.yaml
  - pip
  - pip:
