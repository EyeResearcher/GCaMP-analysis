name: gcamp
channels:
  - pytorch
  - defaults
dependencies:
  - python>=3.10
  - numpy>=1.26
  - scipy>=1.12
  - pandas>=2.1
  - scikit-learn==1.6.1
  - matplotlib>=3.8
  - joblib>=1.3
  - pyyaml>=6.0
  - openpyxl>=3.1
  - tifffile>=2024.2
  - numba>=0.59
  - pip
  - pip:
    - huggingface_hub>=0.30
    # GPU-accelerated DTW (optional — CPU fallback works without it)
    - torch>=2.2
