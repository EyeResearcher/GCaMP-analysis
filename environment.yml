name: gcamp
channels:
  - pytorch
  - defaults
dependencies:
  - python>=3.10
  - numpy>=1.26
  - scipy>=1.12
  - pandas>=2.1
  - scikit-learn>=1.4
  - matplotlib>=3.8
  - joblib>=1.3
  - pyyaml>=6.0
  - openpyxl>=3.1
  - pip
  - pip:
    # GPU-accelerated DTW (optional — CPU fallback works without it)
    - torch>=2.2
